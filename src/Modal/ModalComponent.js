import React from 'react';

const ModalComponent = ({show, onHide, dialogClassName}) {
    show: false,
    onHide: function onHide()=>{setShow(false)}
    dialogClassName="modal-90w"
    aria-labelledby="example-custom-modal-styling-title"
}